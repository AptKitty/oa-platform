<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.dao.AuditLogDao">
    <resultMap id="BaseResultMap" type="com.oa.system.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="module" property="module"/>
        <result column="action" property="action"/>
        <result column="target" property="target"/>
        <result column="detail" property="detail"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_audit_log (user_id, username, module, action, target, detail, ip_address)
        VALUES (#{userId}, #{username}, #{module}, #{action}, #{target}, #{detail}, #{ipAddress})
    </insert>
    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM sys_audit_log ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_audit_log
    </select>
</mapper>
